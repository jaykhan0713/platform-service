app:
  logging:
    mdc:
      headers:
        x-request-id: requestId
        x-user-id: userId
      method: "http.method"
      path: "http.path"
      status: "http.status"
      durationMs: "http.durationMs"