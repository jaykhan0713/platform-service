spring:
  config:
    activate:
      on-profile: prod
  lifecycle:
    timeout-per-shutdown-phase: 30s

management:
  endpoints:
    web:
      exposure:
        include: '*' # expose all only for tuning initially, then keep only what is needed
  endpoint.health.show-details: never
  opentelemetry:
    tracing:
      export:
        otlp:
          endpoint: http://127.0.0.1:4318/v1/traces
  tracing:
    export:
      otlp:
        enabled: true #enable exporting SPANS
    sampling:
      probability: 1.0
  prometheus:
    metrics:
      export:
        enabled: true

server:
  shutdown: graceful
  compression: #if client sends Accept-Encoding: gzip, automatically get this feature during response
    enabled: true
    min-response-size: 1024
    mime-types:
      - application/json