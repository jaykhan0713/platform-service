spring:
  config:
    activate:
      on-profile: dev

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,loggers,prometheus
  prometheus:
    metrics:
      export:
        enabled: true